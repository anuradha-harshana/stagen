image : node:20

pipelines:
  default:
    - step:
        name: Frontend CI

        caches: 
          - node

        script:
          - npm ci
          - npm run type-check
          - npm run lint
          - npm run build  